import type { PageLoad } from './$types';

export const load = (async () => {
    return {
        description: "Soul of Swords, a Vtuber, Cosplayer, Vlogger and streamer from India. Also known as SOS is also India's 2nd babiniku VTuber.",
    };
}) satisfies PageLoad;
